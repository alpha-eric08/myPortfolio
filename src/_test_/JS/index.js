// Mapping through elements of an array
const myArray1 = [
    1,2,3,5,6
];

// To list all the elements of an array, use the .map() method
myArray1.map((element) => {
    console.log(element + 1);
})


myArray1.map((num) => {
      console.log("Number" + num );
})